<span class="inline-flex items-center gap-1">
    Cart
    @if($count > 0)
        <span class="ml-1 text-xs bg-[#FBF9E4] text-[#122C4F] rounded-full px-2 py-0.5">{{ $count }}</span>
    @endif
</span>